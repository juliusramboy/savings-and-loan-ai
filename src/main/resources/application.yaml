server:
  port: 8080

spring:
  application:
    name: savings-and-loan-ai
  ai:
    openai:
      api-key: ${GROQ_API_KEY}
      base-url: ${GROQ_BASE_URL}
      chat:
        options:
          model: ${GROQ_MODEL}

project-a:
  url: ${PROJECT_A_URL}


